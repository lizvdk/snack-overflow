<% votable_css_id = "#{@votable.class.to_s.downcase}-#{@votable.id}" %>

$("#<%= votable_css_id %> .voting").html("<%= escape_javascript (render partial: 'votes', locals: { votable: @votable} ) %>");
